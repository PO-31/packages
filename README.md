# QtReports building packages
This repo contains packages required by Travis CI for building on various OS.
Each branch contains packages for a specific OS
At the moment there only one branch:
 + windows
